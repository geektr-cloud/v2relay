// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// Get a free hosted Postgres database in seconds: `npx create-db`

generator client {
  provider = "prisma-client"
  output   = "../server/generated/prisma"
  runtime  = "cloudflare"
}

generator json {
  provider = "prisma-json-types-generator"
}

generator typescriptInterfacesJson {
  provider    = "prisma-generator-typescript-interfaces"
  output      = "../server/generated/prisma/dto.ts"
  dateType    = "string"
  bigIntType  = "string"
  decimalType = "string"
  relations   = "none"
  exportEnums = true
  prettier    = true
}

datasource db {
  provider = "sqlite"
}

model Provider {
  id            String         @id @default(uuid())
  name          String         @unique
  url           String         @default("")
  syncTags      Boolean        @default(false)
  subscriptions Subscription[]
}

model Subscription {
  id String @id @default(uuid())

  provider   Provider @relation(fields: [providerId], references: [id])
  providerId String

  name      String   @default("")
  remark    String   @default("")
  /// ![string[]]
  urls      Json     @default("[]")
  enabled   Boolean  @default(true)
  price     Float    @default(0)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  nodes     Node[]
}

model Tag {
  id       String @id @default(uuid())
  name     String @unique
  /// ![string[]]
  keywords Json   @default("[]")
}

model Node {
  id String @id @default(uuid())

  subscription   Subscription @relation(fields: [subscriptionId], references: [id])
  subscriptionId String

  /// ![string[]]
  tags Json @default("[]")

  protocol  String @default("")
  name      String @default("")
  remark    String @default("")
  ip        String @default("")
  priceRate Float  @default(1)
  price     Float  @default(0)

  /// ![any]
  connInfo Json @default("{}")
}

model SystemNotice {
  id        String   @id @default(uuid())
  message   String   @default("")
  createdAt DateTime @default(now())
}

model Ruleset {
  id        String   @id @default(uuid())
  name      String   @unique
  url       String   @default("")
  rules     String   @default("")
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

model AppConfig {
  id       String @id @default(uuid())
  name     String @unique
  type     String @default("")
  template String @default("")
  /// ![any]
  config   Json   @default("{}")
}

model Route {
  id   String @id @default(uuid())
  name String @unique
  /// ![string[]]
  rulesets Json @default("[]")
  /// ![import("../../core/nodes/node-filter").Filter]
  filter   Json @default("{\"type\":\"none\"}")
}
